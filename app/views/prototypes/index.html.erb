<main class="main">
  <div class="inner">
    <%# ログイン状態の時は以下を表示する %>
    <% if user_signed_in? %>
      <div class="greeting">
        こんにちは、
        <%= link_to "#{current_user.name}さん", user_path(current_user), class: :greeting__link %>
      </div>
    <% end %>
    <%# // ログイン状態の時は上記を表示する %>
    <div class="card__wrapper">
      <%= render partial: "prototype", collection: @prototypes %>
      <%# 部分テンプレートを使う際は、renderメソッドを使う。
        collection: @(繰り返し表示する要素が入っているインスタンス)
        投稿機能実装後、部分テンプレートでプロトタイプ投稿一覧を表示する %>
    </div>
  </div>
</main>